apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api
  namespace: three-tier
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      role: api
  endpoints:
    - port: http
      path: /healthz
      interval: 30s
